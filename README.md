> [!IMPORTANT]
> Required version: 1.13.0+

## Limitation

On networks requiring authentication (e.g., campus WiFi, hotel networks), temporarily disable the proxy before logging in, then re-enable it after establishing a connection.
